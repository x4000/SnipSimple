<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Dark theme colors -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="ToolbarBackgroundBrush" Color="#2D2D2D" />
    <SolidColorBrush x:Key="ToolbarBorderBrush" Color="#3E3E3E" />
    <SolidColorBrush x:Key="CanvasBackgroundBrush" Color="#1A1A1A" />
    <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="#2D2D2D" />
    <SolidColorBrush x:Key="StatusBarBorderBrush" Color="#3E3E3E" />

    <SolidColorBrush x:Key="PrimaryBrush" Color="#4CC2FF" />
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#3AA8E8" />

    <SolidColorBrush x:Key="SurfaceBrush" Color="#3A3A3A" />
    <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#454545" />
    <SolidColorBrush x:Key="SurfacePressedBrush" Color="#505050" />

    <SolidColorBrush x:Key="TextBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A0A0A0" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="#777777" />

    <SolidColorBrush x:Key="ControlBorderBrush" Color="#555555" />
    <SolidColorBrush x:Key="SwatchBorderBrush" Color="#666666" />

    <SolidColorBrush x:Key="CheckBoxForegroundBrush" Color="#A0A0A0" />

    <!-- Dark scrollbar track and thumb -->
    <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#555555" />
    <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#6E6E6E" />

    <!-- Dark scrollbar style - Thumb -->
    <Style x:Key="DarkScrollBarThumb" TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="thumbBorder"
                            Background="{StaticResource ScrollBarThumbBrush}"
                            CornerRadius="4" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="thumbBorder" Property="Background"
                                    Value="{StaticResource ScrollBarThumbHoverBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Dark vertical scrollbar -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource ScrollBarTrackBrush}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="10" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid>
                                <Border Background="{StaticResource ScrollBarTrackBrush}" CornerRadius="4" />
                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource DarkScrollBarThumb}" />
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="10" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid>
                                <Border Background="{StaticResource ScrollBarTrackBrush}" CornerRadius="4" />
                                <Track x:Name="PART_Track">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource DarkScrollBarThumb}" />
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
